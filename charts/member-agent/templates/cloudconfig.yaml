apiVersion: v1
kind: Secret
metadata:
  name: cloud-config
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  config.json: {{ .Values.config.cloudConfig | toJson | indent 4 | b64enc | quote }}