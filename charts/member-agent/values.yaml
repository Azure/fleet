replicaCount: 1

image:
  repository: ghcr.io/azure/fleet/member-agent
  pullPolicy: Always
  tag: v0.1.0

refreshtoken:
  repository: ghcr.io/azure/fleet/refresh-token
  pullPolicy: Always
  tag: v0.1.0

resources:
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 100m
    memory: 128Mi

tolerations: []

affinity: {}

namespace: 
  fleet-system

config:
  provider: secret
  hubURL : https://127.0.0.1:53528

secret:
  name: "hub-kubeconfig-secret"
  namespace: "default"

azure:
  clientID: ""
