---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: placementresourcesnapshots.placement.kubefleet.dev
spec:
  group: placement.kubefleet.dev
  names:
    categories:
    - kubefleet
    - kubefleet-placement
    kind: PlacementResourceSnapshot
    listKind: PlacementResourceSnapshotList
    plural: placementresourcesnapshots
    singular: placementresourcesnapshot
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PlacementResourceSnapshot is the KubeFleet API that captures the resources selected by a placement policy
          as seen on the hub cluster at a specific point in time. It is referenced by other KubeFleet APIs
          to enable consistent rollouts of resources across multiple member clusters in the fleet.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: The spec of a placement resource snapshot.
            properties:
              resources:
                description: The manifests of the resources selected by the owner
                  placement policy at the time of snapshot creation.
                items:
                  properties:
                    additionalInfo:
                      additionalProperties:
                        format: byte
                        type: string
                      description: Additional information associated with the resource,
                        if any.
                      type: object
                    identifier:
                      description: The identifier of the resource.
                      properties:
                        apiGroup:
                          type: string
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          description: The name of the referenced object.
                          type: string
                        namespace:
                          description: |-
                            The namespace of the referenced object.

                            If the object is cluster-scoped, this field should be left empty.
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      type: object
                    manifest:
                      description: The manifest of the resource. It should be a Kubernetes
                        object in YAML or JSON format.
                      type: object
                      x-kubernetes-embedded-resource: true
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - identifier
                  - manifest
                  type: object
                type: array
            type: object
            x-kubernetes-validations:
            - message: the spec field is immutable
              rule: self == oldSelf
        required:
        - spec
        type: object
    served: true
    storage: true
