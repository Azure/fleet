apiVersion: v1
kind: Service
metadata:
  name: {{ include "demofleet.fullname" . }}
  labels:
    {{- include "demofleet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demofleet.selectorLabels" . | nindent 4 }}
