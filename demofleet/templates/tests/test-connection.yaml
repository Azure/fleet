apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demofleet.fullname" . }}-test-connection"
  labels:
    {{- include "demofleet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demofleet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
