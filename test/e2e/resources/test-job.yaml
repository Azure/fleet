apiVersion: batch/v1
kind: Job
metadata:
  name: sleep
spec:
  template:
    spec:
      containers:
        - name: sleep
          image: alpine:3
          command: ["sh", "-c", "sleep 3"]
      restartPolicy: Never
  backoffLimit: 4